import BuyerNavbar from "../../templates/BuyerNav";

const Buyer = (props) => {
    return (
        <div>
            <BuyerNavbar />
            <div className="container">
            </div>
        </div>

    );
};

export default Buyer;
